# Optional, advanced — see docs/modules/05_deployment_mlops.md, task 5.
apiVersion: v1
kind: Service
metadata:
  name: multimodal-ai-assistant
spec:
  selector:
    app: multimodal-ai-assistant
  ports:
    - port: 80
      targetPort: 8000
  # TODO: choose ClusterIP + Ingress, or LoadBalancer, depending on your cloud provider
  type: LoadBalancer
