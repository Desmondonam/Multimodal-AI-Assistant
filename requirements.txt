# --- Core ---
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.4.0

# --- RAG / NLP ---
sentence-transformers==3.0.1
transformers==4.44.2
torch==2.4.0
lancedb==0.10.0
pandas==2.2.2
numpy==1.26.4
langchain-text-splitters==0.2.2
openai==1.42.0          # or swap for an open-source client (e.g. ollama, huggingface_hub)
tiktoken==0.7.0
datasets==2.20.0
peft==0.12.0            # for parameter-efficient fine-tuning (LoRA)
accelerate==0.33.0
evaluate==0.4.2
rouge-score==0.1.2

# --- Image generation ---
diffusers==0.30.0
safetensors==0.4.4
Pillow==10.4.0

# --- Backend API ---
fastapi==0.112.2
uvicorn[standard]==0.30.6
slowapi==0.1.9          # rate limiting
python-jose[cryptography]==3.3.0   # JWT auth
passlib[bcrypt]==1.7.4
redis==5.0.8            # request queue / caching (optional but recommended)
celery==5.4.0           # async task queue for image generation (optional)

# --- Frontend ---
streamlit==1.38.0
gradio==4.44.0

# --- Observability ---
loguru==0.7.2
prometheus-client==0.20.0

# --- Testing & quality ---
pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2
ruff==0.6.4
mypy==1.11.2
